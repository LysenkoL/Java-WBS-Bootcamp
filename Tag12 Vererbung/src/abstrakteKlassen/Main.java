package abstrakteKlassen;

public class Main {
    public static void main(String[] args) {
         Wurzel wurzel = new Ast(); // Wie wir es kennen: Wir betrachten das Ast-Objekt mit dem Typ Wurzel.
        // Wurzel wurzel = new Wurzel(); // Nicht erlaubt, weil die Klasse abstrakt ist
    }
}
