

// Enums werden verwendet wenn eine feste Menge von Konstanten Werte nemötigt wird.
// In unserem Beispiel repräsentieren die Werte Scheierigkeitsgrade eines Spiels.

public enum SGrad {
    LEICHT,
    MITTEL,
    SCHWER
    // Enum_Namen sind case-sensitive, das bedeutet das "leicht" nicht gleich "LEICHT" ist, es wird nicht
    // als gültige Wert gesehen im Enum.
}